# Dependencies for the Navoiy TTS service (Dockerfile.tts), which runs on a
# CUDA GPU and cannot share requirements.txt's CPU-only torch build.
#
# CosyVoice has its own requirements.txt (WeTextProcessing, onnxruntime, etc.)
# that must be installed separately after cloning it — see setup_tts_model.py
# and the README's Text-to-speech section. This file only covers what app.py
# itself imports directly.

accelerate>=1.9,<2
fastapi>=0.116,<1
huggingface-hub>=0.34,<1
numpy>=1.26,<3
python-dotenv>=1.1,<2
python-multipart>=0.0.20,<1
torch>=2.2,<3
torchaudio>=2.2,<3
transformers>=4.54,<5
uvicorn[standard]>=0.35,<1
